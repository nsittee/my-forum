import { createTheme } from "@material-ui/core"

const theme = createTheme({
  palette: {
    primary: {
      main: '#2196f3'
    },
    secondary: {
      main: '#FFFFFF'
    },
  },
})

export default theme