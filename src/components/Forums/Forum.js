import React from 'react';
import PropTypes from 'prop-types';

const Forum = props => {
    return (
        <div>
            <p>{props.title}</p>
        </div>
    )
};

Forum.propTypes = {

};

export default Forum;
