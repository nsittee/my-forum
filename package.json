{
  "name": "express-back-end",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client:install": "cd src/client/ && npm ci",
    "client:build": "cd src/client/ && npm run build && mv build ../../dist/",
    "client:dev": "cd src/client/ && npm start",
    "server:install": "npm ci",
    "server:build": "tsc",
    "start": "NODE_ENV=production node dist/server.js",
    "server:dev": "nodemon dist/server.js",
    "all:install": "npm run server:install && npm run client:install",
    "dev": "concurrently \"npm run client:dev\" \"npm run server:dev\"",
    "clean": "rm -rf node_modules && rm -rf src/client/node_modules"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.22",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@types/node": "^16.4.11",
    "typescript": "^4.3.5"
  }
}